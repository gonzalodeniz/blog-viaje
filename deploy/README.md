# deploy

`docker-compose.yml` (nginx, backend, worker, postgres, certbot), configuración de nginx (TLS, cabeceras de seguridad, rate limiting, `X-Accel-Redirect`) y scripts de operación. Pendiente de inicializar — ver [TASK-R1-001](../tasks/R1/TASK-R1-001.md) y SPEC-MASTER §7.2/§13.
